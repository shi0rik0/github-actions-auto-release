on:
  push:
    tags:
    - 'v*.*.*'
jobs:
  build-and-publish:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: 'actions/checkout@v4'
    - run: 'make'
    - run: 'gh release create ${{ github.ref_name }} a.out'
